# Hot

`#[Hot]` 可用于函数和方法，用来提示 C++ 编译器：这个函数属于程序中的高频执行路径，应优先考虑执行速度。

```php
namespace App;

use \Hot;

#[Hot]
function decodePacket(string $packet): array
{
    // 高频路径
    return [];
}

class Router
{
    #[Hot]
    public function dispatch(string $path): void
    {
        // 高频路径
    }
}
```

`Hot` 是纯编译期注解。程序运行时不会读取 Attribute、不会使用反射，也不会增加动态调用；TypePHP 会把它直接转换为底层 C++ 函数的优化标记，因此注解机制本身是零运行时开销。

在 GCC 和 Clang 中，它会映射为 `__attribute__((hot))`。编译器可据此更积极地优化函数，并根据工具链和目标平台调整代码布局。其他不支持该能力的 C++ 编译器会安全地忽略此提示。

## 何时使用

适合标记经过性能分析确认、调用频率很高的核心路径，例如请求分发、协议解析、序列化或数值计算。它只是优化提示，不改变 PHP 语义，也不保证一定内联函数。

不建议仅凭猜测大量添加 `Hot`。错误的提示可能增大代码体积，甚至破坏编译器原本更准确的优化决策。优先使用性能分析数据决定标记位置。

即使未启用 LTO，编译器仍能在编译该函数实现的 `.cc` 文件时使用此提示；但跨文件调用点的进一步优化能力仍取决于 LTO、编译器和链接器。

`Hot` 不能与 [`Cold`](cold.md) 同时用于同一个函数或方法，否则编译会失败。

## Library 项目

以 `-m lib` 编译时，`Hot` 会保留在自动生成的 library stub 中。库实现本身在构建时接受该优化提示；使用该 stub 的其他 TypePHP 项目不需要进行运行时处理。
