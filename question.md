## AOT 编译器与 3.2 加密器是否兼容

兼容。可同时使用`AOT`编译器和`3.2`加密器。支持静态编译的文件生成二进制原生指令，存在不支持语法的文件依然可使用`ZendVM`动态加载，使用`3.2`加密器对`PHP`代码加密。

## AOT 编译器是开源免费的吗

不是。`AOT` 编译器是商业软件，不提供源代码。对于非商业应用，可免费使用，无需付费。无论是个人开发者，还是企业、政府、事业单位、学校、公益组织等在非盈利目的的项目中均可免费使用`AOT` 编译器。

## 已购买`Swoole Compiler`客户是否需要再次购买`AOT`编译器

不需要。`AOT`编译器是`Swoole Compiler`的`4.0`升级版。已购买`Swoole Compiler`客户将自动获得`AOT`编译器软件的商业授权，可合法用于商业项目。

若使用`AOT`编译器过程中需要我方团队提供技术支持，仅需支付延长技术支持服务的相关费用。